import React from 'react'
import Top10Item from './Top10item'

const Top10 = ({playlist}) => {

    console.log("passed to Top10 = " + playlist);

    // const individualTop10items = playlist.track.map( (songObj, i) => {
    //     return(
    //         <Top10Item 
            
    //         rank={i} 
    //         songName={songObj.name} songArtist={songObj.artists}
    //         >

    //         </Top10Item>
        // )
    // })

    return(
        <>
            bob  
        </>
    )
}

export default Top10

